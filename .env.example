# MongoDB Configuration (Optional - Defaults to localhost)
# Get your MongoDB connection string from MongoDB Atlas or use local MongoDB
DB_URI=mongodb://localhost:27017
DB_NAME=musiclearn_studio

# JWT Configuration (Optional - Has development defaults)
# Generate a secure random key for production: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-must-be-at-least-512-bits-long-please-change-this
JWT_EXPIRATION_MS=86400000

# Admin Account Seeding (Optional - Default admin account)
# This admin account will be automatically created on first startup if it doesn't exist
ADMIN_NAME=Admin User
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@musiclearn.studio
ADMIN_PASSWORD=Admin@123456

# Cloudflare R2 Storage (Optional - For file uploads)
# Get credentials from Cloudflare Dashboard > R2 > API Tokens
R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=musiclearn-storage
R2_PUBLIC_URL=https://pub-xxxxxxxxxxxxx.r2.dev

# Notes:
# - All environment variables above have default values and are OPTIONAL
# - The application will start without a .env file using development defaults
# - MongoDB defaults to localhost:27017 with database name "musiclearn_studio"
# - JWT uses a dummy key for development (secure enough for local testing)
# - R2 Storage will be DISABLED if credentials are missing (file uploads won't work)
# - For PRODUCTION, you MUST set all values properly for security
